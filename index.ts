/**
 * @format
 */

import {AppRegistry} from 'react-native';
import LiquidSwipe from './src/LiquidSwipe';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => LiquidSwipe);
